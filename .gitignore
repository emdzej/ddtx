node_modules/
dist/
.turbo/
*.tsbuildinfo

# The ECU database is not ours to redistribute (see docs/plan.md §6.4)
data/

# Golden vectors derived from the ECU database (regenerate with tools/golden)
tools/golden/vectors/db.json
__pycache__/
*.pyc

# Compiled plugin modules and the bundle the host serves. Both are build output:
# `pnpm plugins:build` regenerates them in about a second.
packages/plugins/*/build/
apps/web/public/plugins/

# Release packaging output.
out/
